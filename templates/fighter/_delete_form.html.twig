<form method="post" action="{{ path('app_fighter_delete', {'id': fighter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fighter.id) }}">
    <button class="btn">Delete</button>
</form>
